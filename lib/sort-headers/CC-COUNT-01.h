#include "CC-COUNT-02.h"
#include "CC-COUNT-03.h"
#include "CC-COUNT-04.h"
#include "CC-COUNT-05.h"

void countingSort(long int *Integers)
{
    // int comparations = 0, copies = 0;  // will be added the counting of the comparison and copies
    _countingSort(Integers);
}
